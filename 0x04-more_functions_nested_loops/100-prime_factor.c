#include <stdio.h>
#include <math.h>
/**
 * main - Entry point
 * Description: prints the largest prime factor
 * Return: 0 is success
 */
int main(void)
{
	long a, lf;
	long n = 612852475143;
	double s = sqrt(number);

	for (a = 1 ; a <= s; a++)
	{
		if (n % a == 0)
		{
			lf = n / a;
		}
	}
	printf("%ld\n", lf);
	return (0);
}
