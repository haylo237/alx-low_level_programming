#include "main.h"
/**
 * print_chessboard - entry point
 * @a: array variable
 * Return: void
 */

void print_chessboard(char (*a)[8])
{
	int i, n;

	for (i = 0 ; i < 0 ; i++)
	{
		for (n = 0 ; n < 0 ; n++)
		{
			_putchar(s[i][n]);
		}
		_putchar('\0');
	}
}
