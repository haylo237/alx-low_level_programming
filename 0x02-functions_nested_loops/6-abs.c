#include "main.h"
/**
 * _abs - The function that runs
 * @i: the variable to be checked
 * Return: Return are absolute values
 */
int _abs(int i)
{
	if (i < 0)
	{
		return (i * -1);
	}
	else
	{
		return (i);
	}
}
